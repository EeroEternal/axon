# `@axon/core`

Registry, connection manager, session manager, event bus, permission broker, workspace sandbox.

**Phase 1** — no agent-specific logic; depends on `@axon/acp-client` + `@axon/protocol`.
